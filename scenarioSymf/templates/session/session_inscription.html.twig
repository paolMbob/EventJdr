{% extends 'base.html.twig' %}
    {% block contener %}
        {{ parent() }}
        {{ form_start(formulaire) }}
            {{ form_widget(formulaire) }}
            <input type=submit value="Participer"/>
        {{ form_end(formulaire) }}
    {% endblock %}
